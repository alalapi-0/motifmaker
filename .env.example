# API_TITLE 控制 FastAPI 文档标题，生产可根据品牌定制。
API_TITLE=MotifMaker API
# API_VERSION 记录当前后端版本号，便于部署区分。
API_VERSION=0.2.0
# ALLOWED_ORIGINS 允许的前端域名列表，多个值用逗号分隔。
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
# OUTPUT_DIR 渲染产物输出目录，默认为仓库下的 outputs。
OUTPUT_DIR=outputs
# PROJECTS_DIR 工程 JSON 存储目录，默认为仓库下的 projects。
PROJECTS_DIR=projects
# RATE_LIMIT_RPS 每个 IP 每秒允许的最大请求次数，开发环境建议保持较小值。
RATE_LIMIT_RPS=2
# LOG_LEVEL 控制日志等级，可选 INFO/DEBUG/WARNING/ERROR。
LOG_LEVEL=INFO
